import type { Post } from '../types';
import { mockUsers, mockProjects } from './mockData';

export const mockPosts: Post[] = [
  {
    id: 'post1',
    authorId: mockUsers[0].id,
    authorName: mockUsers[0].name,
    authorAvatar: mockUsers[0].avatar,
    authorRole: mockUsers[0].role,
    authorHeadline: mockUsers[0].bio,
    type: 'build_in_public',
    content: "Just shipped the MVP for MediVision AI! 🚀 We've integrated the new diagnostic models and initial tests show a 15% improvement in accuracy over traditional baseline methods.",
    projectId: mockProjects[0].id,
    projectName: mockProjects[0].name,
    tags: ['buildinpublic', 'ai', 'healthcare'],
    likesCount: 142,
    commentsCount: 24,
    sharesCount: 12,
    isLikedByMe: false,
    isSavedByMe: false,
    labxPointsEarned: 1500,
    milestoneTitle: 'MVP Released',
    createdAt: new Date(Date.now() - 1000 * 60 * 60 * 2).toISOString(), // 2 hours ago
    updatedAt: new Date(Date.now() - 1000 * 60 * 60 * 2).toISOString(),
  },
  {
    id: 'post2',
    authorId: mockUsers[1].id,
    authorName: mockUsers[1].name,
    authorAvatar: mockUsers[1].avatar,
    authorRole: mockUsers[1].role,
    authorHeadline: mockUsers[1].bio,
    type: 'project_update',
    content: "EcoTrack has just reached 10 active B2B pilots! Thank you to the LabX community for the constant feedback on our dashboard UI.",
    projectId: mockProjects[1].id,
    projectName: mockProjects[1].name,
    tags: ['milestone', 'climatetech', 'b2b'],
    likesCount: 89,
    commentsCount: 12,
    sharesCount: 3,
    isLikedByMe: true,
    isSavedByMe: false,
    labxPointsEarned: 500,
    createdAt: new Date(Date.now() - 1000 * 60 * 60 * 24).toISOString(), // 1 day ago
    updatedAt: new Date(Date.now() - 1000 * 60 * 60 * 24).toISOString(),
  },
  {
    id: 'post3',
    authorId: mockUsers[2].id,
    authorName: mockUsers[2].name,
    authorAvatar: mockUsers[2].avatar,
    authorRole: mockUsers[2].role,
    authorHeadline: mockUsers[2].bio,
    type: 'question',
    content: "Has anyone successfully implemented Web3auth with Next.js 14 App Router? I'm running into hydration issues with the provider wrapper. Any examples would be greatly appreciated!",
    tags: ['web3', 'nextjs', 'help'],
    likesCount: 15,
    commentsCount: 8,
    sharesCount: 0,
    isLikedByMe: false,
    isSavedByMe: true,
    createdAt: new Date(Date.now() - 1000 * 60 * 60 * 5).toISOString(),
    updatedAt: new Date(Date.now() - 1000 * 60 * 60 * 5).toISOString(),
  }
];
